
namespace KNN.Data
{
    /// <summary>
    /// Enum types to mimic the there different feature types that the *.names file can contain.
    /// </summary>
    public enum Types {
        Continuous,
        Discrete,
        Output
    }
}